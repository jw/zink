setup:
  config:
    MAILGUN_HOST: foobar
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - ./deployment-tasks.sh
run:
  web: gunicorn elevenbits.wsgi:application --bind 0.0.0.0:$PORT