language: python

env:
  - DATABASE_URL=postgres://zink:s3cr3t@localhost:5432/zink SECRET_KEY=a8q#701v8+(s%1dpmm9c=4-df5*3jcg(_aqyxt%6(os%mrpd5= DJANGO=2.0.2 DB=postgres

python:
  - "3.6"

services: postgresql

before_script:
  - psql -c "create role zink with createdb login password 's3cr3t'" -U postgres;
  - psql -c "create database zink owner zink;" -U postgres

script:
  - python setup.py test

install: "make"

script:
  - make test
