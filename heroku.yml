build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python manage.py collectstatic --noinput --clear
    - python manage.py compress --force -v 2
run:
  web: gunicorn elevenbits.wsgi:application --bind 0.0.0.0:$PORT
