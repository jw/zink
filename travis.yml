language: python

python:
  - "2.7"

services: postgresql

script:
  - python setup.py test

env:
  - DJANGO=1.6.1 DB=postgres

install:
  - pip install .
  - pip install -r requirements.txt --use-mirrors
