build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - python manage.py collectstatic --noinput --clear
    - python manage.py compress -v 2
run:
  web: gunicorn zink.wsgi:application --bind 0.0.0.0:$PORT
